# Installation

The user-facing setup flow has been merged into [Getting Started](/en/guide/getting-started).

The recommended path now is to use the MCP directly with `npx`, without a separate installation step.
